{
  "cleanUrls": true,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/nexadata-portfolio/browser",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
